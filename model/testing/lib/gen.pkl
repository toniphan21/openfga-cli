// Code generated by fga generate pkl v`dev` (commit: `5b6ae77`, date: `2025-04-25 18:45:16`) DO NOT EDIT.

abstract class BaseProject {
  id: String
  hidden type: String = "project"
  hidden fgaType: String = "project"

  function toFGAType() = "\(type):\(id)"
  function toFGATypeWith(relation: String) = "\(type):\(id)#\(relation)"
}

abstract class BaseTenant {
  id: String
  hidden type: String = "tenant"
  hidden fgaType: String = "tenant"

  function toFGAType() = "\(type):\(id)"
  function toFGATypeWith(relation: String) = "\(type):\(id)#\(relation)"

  function relation_tenant(i: BaseProject): Mapping = new Mapping {
    ["user"] = toFGAType()
    ["relation"] = "tenant"
    ["object"] = i.toFGAType()
  }

  function assert_project(
    name: String,
    object: BaseProject,
    customAssertions: BaseProjectAssertions,
    asserts: (BaseProjectAssertions) -> Listing
  ): Mapping = new Mapping {
    ["name"] = name
    ["check"] = new Listing {
      new Mapping {
        ["user"] = toFGAType()
        ["object"] = object.toFGAType()
        ["assertions"] = asserts.apply(customAssertions)
          .fold((new Mapping {}).toMap(), (r: Map, m: Mapping) -> r + m.toMap())
      }
    }
  }

  function assert_list_objects_project(
    name: String,
    customListObjects: BaseProjectListObjects,
    listObjects: (BaseProjectListObjects) -> Listing
  ): Mapping = new Mapping {
    ["name"] = name
    ["list_objects"] = new Listing {
      new Mapping {
        ["user"] = toFGAType()
        ["type"] = "project"
        ["assertions"] = listObjects.apply(customListObjects)
          .fold((new Mapping {}).toMap(), (r: Map, m: Mapping) -> r + m.toMap())
      }
    }
  }
}

abstract class BaseUser {
  id: String
  hidden type: String = "user"
  hidden fgaType: String = "user"

  function toFGAType() = "\(type):\(id)"
  function toFGATypeWith(relation: String) = "\(type):\(id)#\(relation)"

  function relation_admin(i: BaseTenant): Mapping = new Mapping {
    ["user"] = toFGAType()
    ["relation"] = "admin"
    ["object"] = i.toFGAType()
  }

  function relation_member(i: BaseTenant): Mapping = new Mapping {
    ["user"] = toFGAType()
    ["relation"] = "member"
    ["object"] = i.toFGAType()
  }

  function relation_owner(i: BaseProject): Mapping = new Mapping {
    ["user"] = toFGAType()
    ["relation"] = "owner"
    ["object"] = i.toFGAType()
  }

  function assert_tenant(
    name: String,
    object: BaseTenant,
    customAssertions: BaseTenantAssertions,
    asserts: (BaseTenantAssertions) -> Listing
  ): Mapping = new Mapping {
    ["name"] = name
    ["check"] = new Listing {
      new Mapping {
        ["user"] = toFGAType()
        ["object"] = object.toFGAType()
        ["assertions"] = asserts.apply(customAssertions)
          .fold((new Mapping {}).toMap(), (r: Map, m: Mapping) -> r + m.toMap())
      }
    }
  }

  function assert_project(
    name: String,
    object: BaseProject,
    customAssertions: BaseProjectAssertions,
    asserts: (BaseProjectAssertions) -> Listing
  ): Mapping = new Mapping {
    ["name"] = name
    ["check"] = new Listing {
      new Mapping {
        ["user"] = toFGAType()
        ["object"] = object.toFGAType()
        ["assertions"] = asserts.apply(customAssertions)
          .fold((new Mapping {}).toMap(), (r: Map, m: Mapping) -> r + m.toMap())
      }
    }
  }

  function assert_list_objects_tenant(
    name: String,
    customListObjects: BaseTenantListObjects,
    listObjects: (BaseTenantListObjects) -> Listing
  ): Mapping = new Mapping {
    ["name"] = name
    ["list_objects"] = new Listing {
      new Mapping {
        ["user"] = toFGAType()
        ["type"] = "tenant"
        ["assertions"] = listObjects.apply(customListObjects)
          .fold((new Mapping {}).toMap(), (r: Map, m: Mapping) -> r + m.toMap())
      }
    }
  }

  function assert_list_objects_project(
    name: String,
    customListObjects: BaseProjectListObjects,
    listObjects: (BaseProjectListObjects) -> Listing
  ): Mapping = new Mapping {
    ["name"] = name
    ["list_objects"] = new Listing {
      new Mapping {
        ["user"] = toFGAType()
        ["type"] = "project"
        ["assertions"] = listObjects.apply(customListObjects)
          .fold((new Mapping {}).toMap(), (r: Map, m: Mapping) -> r + m.toMap())
      }
    }
  }
}

abstract class BaseProjectAssertions {
  can_read = can_read("")
  function can_read(reason: String) = new Mapping { ["can_read"] = true }
  cannot_read = cannot_read("")
  function cannot_read(reason: String) = new Mapping { ["can_read"] = false }

  has_relation_can_read = has_relation_can_read("")
  function has_relation_can_read(reason: String) = new Mapping { ["can_read"] = true }
  do_not_have_relation_can_read = do_not_have_relation_can_read("")
  function do_not_have_relation_can_read(reason: String) = new Mapping { ["can_read"] = false }

  can_write = can_write("")
  function can_write(reason: String) = new Mapping { ["can_write"] = true }
  cannot_write = cannot_write("")
  function cannot_write(reason: String) = new Mapping { ["can_write"] = false }

  has_relation_can_write = has_relation_can_write("")
  function has_relation_can_write(reason: String) = new Mapping { ["can_write"] = true }
  do_not_have_relation_can_write = do_not_have_relation_can_write("")
  function do_not_have_relation_can_write(reason: String) = new Mapping { ["can_write"] = false }

  should_be_owner = should_be_owner("")
  function should_be_owner(reason: String) = new Mapping { ["owner"] = true }
  should_not_be_owner = should_not_be_owner("")
  function should_not_be_owner(reason: String) = new Mapping { ["owner"] = false }

  has_relation_owner = has_relation_owner("")
  function has_relation_owner(reason: String) = new Mapping { ["owner"] = true }
  do_not_have_relation_owner = do_not_have_relation_owner("")
  function do_not_have_relation_owner(reason: String) = new Mapping { ["owner"] = false }

  can_tenant = can_tenant("")
  function can_tenant(reason: String) = new Mapping { ["tenant"] = true }
  cannot_tenant = cannot_tenant("")
  function cannot_tenant(reason: String) = new Mapping { ["tenant"] = false }

  has_relation_tenant = has_relation_tenant("")
  function has_relation_tenant(reason: String) = new Mapping { ["tenant"] = true }
  do_not_have_relation_tenant = do_not_have_relation_tenant("")
  function do_not_have_relation_tenant(reason: String) = new Mapping { ["tenant"] = false }

  has_all_relations = has_all_relations("")
  function has_all_relations(reason: String) = new Mapping {
    ["can_read"] = true
    ["can_write"] = true
    ["owner"] = true
    ["tenant"] = true
  }
  do_not_have_any_relations = do_not_have_any_relations("")
  function do_not_have_any_relations(reason: String) = new Mapping {
    ["can_read"] = false
    ["can_write"] = false
    ["owner"] = false
    ["tenant"] = false
  }

  should_be_everything = should_be_everything("")
  function should_be_everything(reason: String) = new Mapping {
    ["owner"] = true
  }
  should_not_be_anything = should_not_be_anything("")
  function should_not_be_anything(reason: String) = new Mapping {
    ["owner"] = false
  }

  can_do_everything = can_do_everything("")
  function can_do_everything(reason: String) = new Mapping {
    ["can_read"] = true
    ["can_write"] = true
    ["tenant"] = true
  }
  cannot_do_anything = cannot_do_anything("")
  function cannot_do_anything(reason: String) = new Mapping {
    ["can_read"] = false
    ["can_write"] = false
    ["tenant"] = false
  }
}

abstract class BaseTenantAssertions {
  can_admin = can_admin("")
  function can_admin(reason: String) = new Mapping { ["admin"] = true }
  cannot_admin = cannot_admin("")
  function cannot_admin(reason: String) = new Mapping { ["admin"] = false }

  has_relation_admin = has_relation_admin("")
  function has_relation_admin(reason: String) = new Mapping { ["admin"] = true }
  do_not_have_relation_admin = do_not_have_relation_admin("")
  function do_not_have_relation_admin(reason: String) = new Mapping { ["admin"] = false }

  should_be_member = should_be_member("")
  function should_be_member(reason: String) = new Mapping { ["member"] = true }
  should_not_be_member = should_not_be_member("")
  function should_not_be_member(reason: String) = new Mapping { ["member"] = false }

  has_relation_member = has_relation_member("")
  function has_relation_member(reason: String) = new Mapping { ["member"] = true }
  do_not_have_relation_member = do_not_have_relation_member("")
  function do_not_have_relation_member(reason: String) = new Mapping { ["member"] = false }

  has_all_relations = has_all_relations("")
  function has_all_relations(reason: String) = new Mapping {
    ["admin"] = true
    ["member"] = true
  }
  do_not_have_any_relations = do_not_have_any_relations("")
  function do_not_have_any_relations(reason: String) = new Mapping {
    ["admin"] = false
    ["member"] = false
  }

  should_be_everything = should_be_everything("")
  function should_be_everything(reason: String) = new Mapping {
    ["member"] = true
  }
  should_not_be_anything = should_not_be_anything("")
  function should_not_be_anything(reason: String) = new Mapping {
    ["member"] = false
  }

  can_do_everything = can_do_everything("")
  function can_do_everything(reason: String) = new Mapping {
    ["admin"] = true
  }
  cannot_do_anything = cannot_do_anything("")
  function cannot_do_anything(reason: String) = new Mapping {
    ["admin"] = false
  }
}

abstract class BaseProjectListObjects {
  function is_owner_of(items: List<BaseProject>) = new Mapping {
    ["owner"] = items.map((item: BaseProject) -> item.toFGAType()).toListing()
  }

  function has_relation_owner(items: List<BaseProject>) = new Mapping {
    ["owner"] = items.map((item: BaseProject) -> item.toFGAType()).toListing()
  }

  function is_tenant_of(items: List<BaseProject>) = new Mapping {
    ["tenant"] = items.map((item: BaseProject) -> item.toFGAType()).toListing()
  }

  function has_relation_tenant(items: List<BaseProject>) = new Mapping {
    ["tenant"] = items.map((item: BaseProject) -> item.toFGAType()).toListing()
  }

  function can_read(items: List<BaseProject>) = new Mapping {
    ["can_read"] = items.map((item: BaseProject) -> item.toFGAType()).toListing()
  }

  function has_relation_can_read(items: List<BaseProject>) = new Mapping {
    ["can_read"] = items.map((item: BaseProject) -> item.toFGAType()).toListing()
  }

  function can_write(items: List<BaseProject>) = new Mapping {
    ["can_write"] = items.map((item: BaseProject) -> item.toFGAType()).toListing()
  }

  function has_relation_can_write(items: List<BaseProject>) = new Mapping {
    ["can_write"] = items.map((item: BaseProject) -> item.toFGAType()).toListing()
  }
}

abstract class BaseTenantListObjects {
  function is_admin_of(items: List<BaseTenant>) = new Mapping {
    ["admin"] = items.map((item: BaseTenant) -> item.toFGAType()).toListing()
  }

  function has_relation_admin(items: List<BaseTenant>) = new Mapping {
    ["admin"] = items.map((item: BaseTenant) -> item.toFGAType()).toListing()
  }

  function is_member_of(items: List<BaseTenant>) = new Mapping {
    ["member"] = items.map((item: BaseTenant) -> item.toFGAType()).toListing()
  }

  function has_relation_member(items: List<BaseTenant>) = new Mapping {
    ["member"] = items.map((item: BaseTenant) -> item.toFGAType()).toListing()
  }
}
